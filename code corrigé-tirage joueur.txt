let joueurActif1 = prompt("Entrez le nom du premier joueur :");
let joueurActif2 = prompt("Entrez le nom du deuxième joueur :");
let joueurGagnant;
let joueurPerdant;
var roll;

function déLancé() {
  roll = Math.floor(Math.random() * 6) + 1;
  console.log("Le résultat du lancé de dé est : " + roll);
}

var tirageJoueur = function () {
  joueurGagnant = Math.random() < 0.5 ? joueurActif1 : joueurActif2;
  joueurPerdant = joueurGagnant === joueurActif1 ? joueurActif2 : joueurActif1;
  console.log("Le joueur tiré au sort est : " + joueurGagnant);
  console.log("Le joueur perdant est : " + joueurPerdant);
  alert("Le joueur tiré au sort est : " + joueurGagnant);
  déLancé();
  return joueurGagnant;
};

tirageJoueur();
